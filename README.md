# PPI
Private Program Installer

## Why PPI?
PPI can support: .exe, .tar.*, .tar, .deb, .rpm, .appimage.

## Installing on Linux
1. Download the file `install.sh`
2. In the terminal, run `sudo bash install.sh`
3. You will be guided through the installing process.
## Installing on Windows and MacOS
Coming soon.
